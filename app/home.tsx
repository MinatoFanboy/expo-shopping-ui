import React, { FC } from 'react';

/** React Native */
import { Text, View } from 'react-native';

const home: FC = () => {
    return (
        <View>
            <Text>Home Page</Text>
        </View>
    );
};

export default home;
